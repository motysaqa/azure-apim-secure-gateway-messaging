{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Azure.Messaging.ServiceBus": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Bookings": "Data Source=bookings.db"
  },
  "ServiceBus": {
    "//": "Leave FullyQualifiedNamespace empty for local development; the app then records messages in memory instead of publishing. Never put a connection string here - access is by managed identity.",
    "FullyQualifiedNamespace": "",
    "QueueName": "booking-created"
  },
  "Gateway": {
    "//": "Set by APIM from a Key Vault-backed named value. Empty locally so the API runs without a gateway in front of it.",
    "BackendKey": ""
  },
  "Outbox": {
    "PollIntervalSeconds": 2,
    "BatchSize": 20,
    "MaxAttempts": 10
  }
}
