{
  "//": "Copy to appsettings.Production.json (gitignored) or, better, set these as App Service application settings. No secrets belong in this file: Service Bus and Key Vault are reached with the App Service managed identity, so there is nothing to store.",
  "ConnectionStrings": {
    "Bookings": "Data Source=/home/data/bookings.db"
  },
  "ServiceBus": {
    "FullyQualifiedNamespace": "sb-booking-<suffix>.servicebus.windows.net",
    "QueueName": "booking-created"
  },
  "Gateway": {
    "//": "In Azure this is an App Service setting sourced from Key Vault: @Microsoft.KeyVault(SecretUri=https://kv-<suffix>.vault.azure.net/secrets/backend-key/). Never a literal value.",
    "BackendKey": "@Microsoft.KeyVault(SecretUri=https://kv-<suffix>.vault.azure.net/secrets/backend-key/)"
  },
  "Outbox": {
    "PollIntervalSeconds": 2,
    "BatchSize": 20,
    "MaxAttempts": 10
  }
}
