{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information",
      "Azure.Messaging.ServiceBus": "Warning"
    }
  },
  "ConnectionStrings": {
    "WorkerStore": "Data Source=worker.db"
  },
  "ServiceBus": {
    "//": "Leave FullyQualifiedNamespace empty to run the worker idle locally. Access is by managed identity; there is deliberately no connection string setting.",
    "FullyQualifiedNamespace": "",
    "QueueName": "booking-created",
    "MaxConcurrentCalls": 4,
    "PrefetchCount": 10
  }
}
